<UserControl x:Class="ExtremeSignalAppCS.Controls.UnbrokenKMonitor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid Background="{StaticResource PanelDarkBg}">
        <Grid.RowDefinitions>
            <!-- 0: 標題 -->
            <RowDefinition Height="Auto"/>
            <!-- 1: 監控顯示區 -->
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 標題 -->
        <Border Grid.Row="0" BorderBrush="{StaticResource WidgetBorderBrush}" BorderThickness="0,0,0,1" Padding="6,4">
            <TextBlock x:Name="lblTitle" Text="🛡️ 未破分 K 停損監控" Foreground="{StaticResource SciFiGreen}" FontWeight="Bold" FontSize="12"/>
        </Border>

        <!-- 監控顯示區 -->
        <RichTextBox Grid.Row="1" x:Name="txtDisplay" IsReadOnly="True" Background="#1A1A1A" Foreground="#DCDCDC" 
                     FontFamily="Consolas" FontSize="13" BorderThickness="0" Margin="4"
                     VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"/>

    </Grid>
</UserControl>
